#' Simulated latent class data with competing risks
#'
#' @usage data(simu)
#' @format a data frame of 3 latent classes (with unknown label), with 500 subjects, 2 baseline covariates (Y.1 and Y.2), time of competing risks (ftime), and failure types (fstatus). Failure types include type 1, 2, censored 0, or missing NA.
"simu"
